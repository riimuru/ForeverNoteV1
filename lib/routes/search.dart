import 'package:flutter/material.dart';

class Search extends StatelessWidget {
  @override
  Widget build(BuildContext context){
    return const Text("This is the search page", style: TextStyle(fontSize: 20.0));
  }
}